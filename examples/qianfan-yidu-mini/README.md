# 千帆一渡最小示例

本示例只用于测试短线交易员蒸馏元Skill的规则提取能力。材料包含少量明确筛选条件，以及若干未量化条件。

要求：
- 明确阈值规则的 `rule_type` 必须保留为 `explicit`，`category` 按业务类别填写。
- “热点板块”“技术形态”“大盘环境”只能标为需要结合，或将 `rule_type` 标为 `unresolved`，不得擅自量化。
- 仓位、卖出、止损规则材料未提供，必须将 `rule_type` 标为 `unresolved`。
